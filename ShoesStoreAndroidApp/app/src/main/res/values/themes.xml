<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ShoesStoreAndroidApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Thêm dòng này để đổi màu status bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Hoặc đổi thành màu trắng -->
        <!-- <item name="android:statusBarColor">@android:color/white</item> -->
    </style>

    <style name="Theme.ShoesStoreAndroidApp" parent="Base.Theme.ShoesStoreAndroidApp" />
</resources>
